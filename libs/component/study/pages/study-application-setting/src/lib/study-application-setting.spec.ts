import { studyApplicationSetting } from './study-application-setting';

describe('studyApplicationSetting', () => {
  it('should work', () => {
    expect(studyApplicationSetting()).toEqual('study-application-setting');
  });
});
